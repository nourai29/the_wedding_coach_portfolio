
  # High-Fidelity UI Layout

  This is a code bundle for High-Fidelity UI Layout. The original project is available at https://www.figma.com/design/6pOn4KinYn7nr3R4uzflXx/High-Fidelity-UI-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  